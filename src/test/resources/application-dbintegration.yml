spring:
  datasource:
    driver-class-name: "org.h2.Driver"
    url: "jdbc:h2:mem:db"
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
logging:
  level:
    org:
      hibernate:
        type: trace
